from django.apps import AppConfig

class DocumentGeneratorConfig(AppConfig):
    """
    Configuration class for the 'document_generator' app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'document_generator'
